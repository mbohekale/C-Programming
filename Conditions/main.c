#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,y;
    x=5;
    y=3;
    if(x<y)
        {
        printf("x is less than y\n");
        }
        else
        {
        printf("x is greater than y\n");
        }


    return 0;
}
